// Syntaxis de if
if (condicion) {
    "resultado verdadero";
  } else {
    "resultado falso";
  }

// Operador Ternario
condición ? expr1 : expr2 

// Ejemplo:

var numero = 1;
var resultado = numero === 1 ? "Si soy un uno" : "No, no soy un uno";

// Con este codigo se valida si numero es 1 y si se cumple entra a "S soy un uno"
